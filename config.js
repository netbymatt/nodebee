module.exports = {
	nodeBee: {
		//	Your ecobee developer client ID
		// clientId: '',

		// log API calls for debugging
		// logApiCalls: true,

		// Read only (smartRead) or read-write access (smartWrite) which is unnecessary for this application
		// scope: 'smartWrite',
	},
	db: {
		// database credentials
		// username: '',
		// password: '',

		// database host
		// host: '',

		// database name
		// name: '',

		// maximum number of connections
		// connections: 1,
	},

	// rate to poll the Ecobee api when running the cron task 
	// rate: 60000,
};
